[user]
	email = sang.nguyen@classdojo.com
	name = Sang Nguyen

[core]
    pager = delta

[delta]
    ; side-by-side = true
    ; syntax-theme = Monokai
    features = catppuccin-mocha
    line-numbers = true

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
[push]
	autoSetupRemote = true

[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
    dc = diff --cached
